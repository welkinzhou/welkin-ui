import { withInstall } from "@welkin-ui/utils";

import NavLink from "./src/nav-link.vue";
import NavItem from "./src/link-item.vue";

export const WelNavLink = withInstall(NavLink);

export const WelNavItem = withInstall(NavItem);
