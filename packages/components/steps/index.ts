import { withInstall } from "@welkin-ui/utils";

import Steps from "./src/steps.vue";

import Step from "./src/step.vue";

export const WelSteps = withInstall(Steps);

export const WelStep = withInstall(Step);
