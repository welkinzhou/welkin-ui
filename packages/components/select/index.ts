import { withInstall } from '@welkin-ui/utils'

import Select from './src/select.vue'

import Option from './src/option.vue'

export const WelSelect = withInstall(Select)

export const WelOption = withInstall(Option)
